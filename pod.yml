apiVersion: v1
kind: Pod
metadata:
  name: simple-webapp-color
  labels:
    app: web
spec:
  containers:
  - name: web
    image: mmumshad/simple-webapp-color  # Ajout des deux-points après "image"
    ports:
      - containerPort: 8080
    env:
      - name: APP_COLOR
        value: red

